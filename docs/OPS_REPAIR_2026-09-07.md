# Hermes operations repair — 2026-09-07

## Authorization and boundary

The owner requested a list of the previously observed problems and authorized their repair. Scope: delegated approval wait accounting, checkpoint repository health, Notion MCP credentials, and the duplicate HA event adapters in `havoice`/`herald`. Keep manual approvals and their 900-second timeout, preserve local patches/data/secrets, and do not change HA devices or the separate voice services. [Source: User, 2026-09-07]

## Current result before gateway activation

- **Checkpoints recovered live.** The existing store was missing empty `refs/` skeleton directories. A private tar backup was verified against all 213 original file hashes. Only `refs/heads` and `branches` were added; all original file bytes remained unchanged. Git repository detection and full fsck now pass; the preexisting 27 refs were readable. A new harmless fixture checkpoint was then created and safely restored with the production manager. Nothing in another project was restored or pruned. Code now also invokes the existing skeleton repair before operations on an existing HEAD+objects store; absent repositories are not initialized. [Source: /root/.local/state/hermes-ops-repair-20260907/checkpoint-recovery.json; checkpoint-live-canary.json; tests/tools/test_checkpoint_missing_refs_recovery.py, 2026-09-07]
- **Notion MCP recovered live without restart.** The wrapper expected `/root/.creds/notion`, which was absent, while a valid `NOTION_API_KEY` already existed in the default profile `.env`. The intended token passed the authenticated `/v1/users/me` check; the canonical wrapper file was created privately with mode0600. A real stdio session initialized, listed24 tools and successfully executed `API-get-self`. The resident gateway logged `parked → connected` at13:04:55 UTC. No token value or Notion content is stored in Git. [Source: notion-auth-recovery.json; notion-mcp-readback.json; gateway PID2742508 journal, 2026-09-07]
- **Approval fix tested, activation pending.** A delegated child's configured600-second budget now excludes only time measured inside its own actual human-wait windows. Owner context is propagated to tool workers; sibling approvals in the same chat do not extend this deadline. Existing window ceilings prevent wedged waits from extending it indefinitely. Nonhuman work still times out and both `once` and `deny` resolve by exact request ID. `approvals.timeout=900`, manual policy and child budget600 are unchanged. This is an execution-budget correction, not automatic approval. [Source: tools/approval_human_wait.py; tools/delegate_tool_child_run.py; tests/tools/test_delegation_human_wait_budget.py, 2026-09-07]
- **HA adapter configuration corrected, activation pending.** Only top-level `platforms.homeassistant.enabled=false` was added to `havoice` and `herald`. All other YAML values match the private backups, and each `.env` hash is unchanged. The main profile retains the HA event adapter. Direct HA tools retain their credentials; the separate voice bridge invokes the `havoice` CLI and toolsets rather than relying on this duplicate gateway adapter. No HA server, devices or voice unit was changed/restarted. [Source: ha-adapter-preflight.json; ha-config-readback.json; voice service ExecStart; gateway/config_loader.py and config_env.py, 2026-09-07]

AskChem and GBrain answered real read-only MCP calls during repair; the historical reconnect warnings do not establish a current persistent failure. No unrelated MCP backend was restarted or reconfigured. [Source: resident AskChem stats and GBrain health tool results, 2026-09-07]

## Verification

TDD reproduced the two defects before code changes (2failed/3passed); the dedicated regression files now pass. Expanded final verification ran **69 files / 1110 tests passed / 0 failed / 0 errors / 0 skipped**, in isolated per-file processes with the actual gateway interpreter. It covers the earlier reconciled local fixes plus delegation, approval queues/context/ceilings, checkpoint behavior and rollback contracts. Syntax compilation and diff whitespace checks passed. Full broad-suite green is not claimed. [Source: /root/.local/state/hermes-ops-repair-20260907/regression-final/summary.json, per-file JUnit logs and red-tests.log, 2026-09-07]

Independent static review additionally identified inherited owner-cancellation cleanup and provider-TimeoutError misclassification at this lifetime boundary. Three new tests failed before those repairs; the dedicated files now pass12 tests. Cancellation closes steering, signals stop and defers resource close until the worker settles; the original cancellation is re-raised. Provider TimeoutError no longer becomes a false child-budget timeout. Real tool submissions were inspected for ContextVar propagation, and a second-worker-hop plus actual gateway approval queue/resolve tests verify it. The existing finite window ceiling is retained, not broadened into unlimited overlapping wait exclusion. [Source: static-review.md, review-disposition.md and review-red.log in the private evidence directory; final regression, 2026-09-07]

Test environment caveat: a preexisting `/tmp/.git` caused9 checkpoint fixture failures on both unmodified production and candidate, by making `/tmp` their discovered project root. The same complete checkpoint suite passed53/53 with `TMPDIR=/var/tmp`. The preexisting `/tmp/.git` was preserved; no test assertions were loosened to hide this. One new test was corrected to inspect `--git-dir` rather than bare-ness because the manager intentionally supplies GIT_WORK_TREE. [Source: checkpoint-baseline.log and fresh isolated pytest output, 2026-09-07]

## Activation and follow-up

These code changes and HA adapter flags must be read by a new gateway process. Use the native after-turn graceful restart, not the historical update installer and not an in-turn hard restart. The current loaded gateway remains PID2742508/code1792e07e3a until activation is proved. Its after-turn wait is1800seconds and systemd stop timeout330seconds. Schedule a bounded, self-contained post-activation verifier before requesting reload; report pending honestly until it reads the new PID/code stamp and adapter state. [Source: source read, effective runtime timeout resolver and systemctl, 2026-09-07]

Acceptance after restart: exact expected executable tree; new systemd PID and matching status writer; Telegram connected; main HA connected without duplicate secondary adapters; Notion remains healthy; checkpoint Git fsck and safe fixture check; focused regressions and no newly introduced relevant errors. A human Telegram button round-trip remains a separate acceptance check unless actually exercised. [Source: repair acceptance scope, 2026-09-07]

Private backups/evidence: `/root/.local/state/hermes-ops-repair-20260907/`. Preserve the original checkpoint archive and profile config backups. Roll back only an owned code/config change if necessary; never reinitialize or delete the store to hide an error. [Source: User preservation requirement and verified backup manifest, 2026-09-07]
